CREATE TABLE tarea (
                       id INT PRIMARY KEY AUTO_INCREMENT,
                       nombre VARCHAR(55) NOT NULL,
                       descripcion varchar(255) NOT NULL,
                       fecha_inicio DATE
);